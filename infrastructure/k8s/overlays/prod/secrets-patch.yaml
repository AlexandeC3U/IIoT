# Production Secrets Patch
# IMPORTANT: Replace these with actual secrets from your secret management system
# In production, use:
# - HashiCorp Vault with External Secrets Operator
# - AWS Secrets Manager
# - Azure Key Vault
# - Sealed Secrets

# This file is a TEMPLATE - do not commit real secrets!
apiVersion: v1
kind: Secret
metadata:
  name: nexus-secrets
  namespace: nexus
type: Opaque
stringData:
  # REPLACE THESE WITH ACTUAL PRODUCTION VALUES
  MQTT_USERNAME: "nexus_prod"
  MQTT_PASSWORD: "${NEXUS_MQTT_PASSWORD}"  # Set via CI/CD
  
  TIMESCALEDB_USER: "nexus_ingestion"
  TIMESCALEDB_PASSWORD: "${NEXUS_DB_PASSWORD}"  # Set via CI/CD

---
apiVersion: v1
kind: Secret
metadata:
  name: timescaledb-secrets
  namespace: nexus
type: Opaque
stringData:
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "${POSTGRES_PASSWORD}"  # Set via CI/CD
  POSTGRES_DB: "nexus_historian"

---
apiVersion: v1
kind: Secret
metadata:
  name: emqx-secrets
  namespace: nexus
type: Opaque
stringData:
  EMQX_DASHBOARD__DEFAULT_USERNAME: "admin"
  EMQX_DASHBOARD__DEFAULT_PASSWORD: "${EMQX_ADMIN_PASSWORD}"  # Set via CI/CD

