# Data Ingestion Service - Kubernetes Deployment
# Receives MQTT messages and writes to TimescaleDB
apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-ingestion
  namespace: nexus
  labels:
    app.kubernetes.io/name: data-ingestion
    app.kubernetes.io/component: ingestion
    app.kubernetes.io/part-of: nexus-edge
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: data-ingestion
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: data-ingestion
        app.kubernetes.io/component: ingestion
        app.kubernetes.io/part-of: nexus-edge
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: data-ingestion
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      
      # Wait for dependencies
      initContainers:
        - name: wait-for-emqx
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z emqx 1883; do echo waiting for emqx; sleep 2; done']
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "100m"
              memory: "32Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
        - name: wait-for-timescaledb
          image: busybox:1.36
          command: ['sh', '-c', 'until nc -z timescaledb 5432; do echo waiting for timescaledb; sleep 2; done']
          resources:
            requests:
              cpu: "10m"
              memory: "16Mi"
            limits:
              cpu: "100m"
              memory: "32Mi"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      
      containers:
        - name: data-ingestion
          image: nexus/data-ingestion:latest
          imagePullPolicy: IfNotPresent
          
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          
          env:
            # From ConfigMap
            - name: INGESTION_MQTT_BROKER_URL
              valueFrom:
                configMapKeyRef:
                  name: nexus-config
                  key: MQTT_BROKER
            - name: INGESTION_LOGGING_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: nexus-config
                  key: LOG_LEVEL
            - name: INGESTION_DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: nexus-config
                  key: TIMESCALEDB_HOST
            - name: INGESTION_DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: nexus-config
                  key: TIMESCALEDB_PORT
            - name: INGESTION_DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: nexus-config
                  key: TIMESCALEDB_DATABASE
            # From Secrets
            - name: MQTT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: nexus-secrets
                  key: MQTT_USERNAME
            - name: MQTT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexus-secrets
                  key: MQTT_PASSWORD
            - name: INGESTION_DB_USER
              valueFrom:
                secretKeyRef:
                  name: nexus-secrets
                  key: TIMESCALEDB_USER
            - name: INGESTION_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexus-secrets
                  key: TIMESCALEDB_PASSWORD
            # Instance identifier for shared subscriptions
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: INGESTION_MQTT_CLIENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          
          volumeMounts:
            - name: config
              mountPath: /app/config
              readOnly: true
          
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 30
          
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 3

          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 3
          
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      
      volumes:
        - name: config
          configMap:
            name: data-ingestion-config
      
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: data-ingestion
                topologyKey: kubernetes.io/hostname
      
      terminationGracePeriodSeconds: 60

