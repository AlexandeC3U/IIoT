# PostgreSQL (Config DB) - Service Account
apiVersion: v1
kind: ServiceAccount
metadata:
  name: postgres
  namespace: nexus
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/component: config-database
    app.kubernetes.io/part-of: nexus-edge
