# Gateway Core - Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nexus

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml
  - pdb.yaml

commonLabels:
  app.kubernetes.io/name: gateway-core
  app.kubernetes.io/component: api-gateway
